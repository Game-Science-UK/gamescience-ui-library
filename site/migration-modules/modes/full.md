## Mode instructions — full visual alignment

```text
{{MODE_LABEL}}
```

Behaviour:

1. Audit first
2. Migrate foundations and prove one vertical slice
3. Continue eligible screens only after the first slice passes
4. Remove redundant forks after validation
5. Preserve application logic
6. Stop at unresolved API gaps — do not invent shared components

Full mode is **not** permission for a destructive rewrite.
