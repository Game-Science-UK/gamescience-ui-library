## Context focus — multiple contexts

Selected contexts: {{CONTEXTS}}

Prefer the **participant join flow** as the first vertical slice unless project evidence indicates another safer slice.

After the first slice validates, continue one surface at a time. Do not migrate all contexts in a single overwrite pass.
Keep a single root theme; change context per interface surface.
