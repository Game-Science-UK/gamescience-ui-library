## Context focus — multiple contexts

Selected contexts: {{CONTEXTS}}

These are migration targets, not roles. Prefer the **participant join flow** as
the first vertical slice unless project evidence indicates another safer slice.

After the first slice validates, continue one surface at a time. Do not migrate
all contexts in a single overwrite pass. Keep a single root theme; change
context per interface surface. Split compositions only where jobs, privacy, or
interaction materially differ. Do not force unused contexts into the project.
