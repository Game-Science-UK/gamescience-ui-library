## Context focus — facilitator

Prefer the facilitator lobby as the first vertical slice:

empty lobby → participants joining → readiness → start confirmation

Use `@gamescience/lobby` and `@gamescience/facilitator-shell` where appropriate.
Preserve existing application state and handlers; map them into pattern props.
