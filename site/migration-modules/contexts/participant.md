## Context focus — participant

Prefer the participant join flow as the first vertical slice:

enter code → identity → waiting → reconnecting / disconnected

Use `@gamescience/join-flow` and `@gamescience/participant-shell` where appropriate.
Preserve existing application state and handlers; map them into pattern props.
