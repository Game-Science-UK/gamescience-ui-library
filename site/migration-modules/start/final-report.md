## Final report (start)

Report:

1. Registry version (**{{VERSION}}**)
2. Theme installed
3. Contexts selected and contexts intentionally omitted
4. Registry items installed (shells and patterns matched to selected contexts only)
5. One root `GameScienceProvider` placement
6. How active context is resolved from routes or configuration
7. CSS / font wiring
8. `src/docs/gamescience-ui-contexts.md` created
9. Validation results (typecheck / lint / build)
10. Confirmation that unselected context patterns were not installed by default
